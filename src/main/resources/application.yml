spring:
  application:
    name: fleetops

  datasource:
    url: jdbc:postgresql://localhost:5432/fleetops
    username: your_username
    password: your_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
